# Bridge 网关自身依赖
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12
httpx==0.27.2

# ppt-master 的 SVG→PPTX 与文档转换依赖在 Dockerfile 中通过
# `pip install -r /opt/ppt-master/skills/ppt-master/requirements.txt` 安装。
