# Bridge 网关自身依赖
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12
httpx==0.27.2

# PPTAgent 通过本仓库 integrations/PPTAgent 安装（详见 Dockerfile），
# 不在此处固定版本，避免与上游 PPTAgent 的 pyproject 冲突。
